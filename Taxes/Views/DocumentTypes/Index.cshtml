@model IEnumerable<Taxes.Models.DocumentType>

@{
    ViewBag.Title = "Document Types";
}

<h2>Document Types</h2>

<p>
    @Html.ActionLink("Create New", "Create", new { }, new { @class = "btn btn-primary"})
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DocumentTypeId }, new { @class = "btn btn-warning"})
            @Html.ActionLink("Details", "Details", new { id=item.DocumentTypeId }, new { @class = "btn btn-info" })
            @Html.ActionLink("Delete", "Delete", new { id=item.DocumentTypeId }, new { @class = "btn btn-danger" })
        </td>
    </tr>
}

</table>
